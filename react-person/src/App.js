const React = require('react')
const ResourceForm = require('./pages/resources/form')


const App = React.createClass({
  render() {
    return (
      <div>
        Hello World
        <ResourceForm />
      </div>
    )
  }
})

module.exports = App
